/**
 * Función Suma - Sumar dos numeros
 * Autor: Ruben Felipe Tovar Aviles
 * Fecha: Lunes 01 de abril del 2024
 */

// Como Parametro
function suma(pNumUno, pNumDos){
    let numUno = pNumUno;
    let numDos = pNumDos;
    let sumar;
    sumar = numUno + numDos

    return sumar
}

// Como Expresión
const sumaExpresion = function(pNumUno, pNumDos){
    let numUno = pNumUno
    let numDos = pNumDos
    let sumar
    sumar = numUno + numDos

    return sumar
}